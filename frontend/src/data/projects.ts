import type { Project } from "../types";

/**
 * No verified, business-approved projects have been supplied.
 * Per docs/CONTENT.md, project names, clients, and descriptions
 * require explicit business approval before publishing. The Projects
 * page renders an empty state until real entries are added here.
 */
export const projects: Project[] = [];
